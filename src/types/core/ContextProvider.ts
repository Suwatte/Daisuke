export type ContextProvider = {
  /**
   * For Objects that may be passed back to the Runner, a context object is provided to be used to pass additional info back to the runner
   */
  context?: Record<string, any>;
};
